package tp.pr2.logica;

/**
 * Repesenta la información del color de una ficha. 
 * El enumerado es utilizado para guardar la información
 * de cada posición de los tableros,
 * por lo que contiene también un símbolo para indicar la ausencia de ficha
 * en esa posición. También se utiliza para el color de un jugador. 
 * IMPORTANTE: en la documentación aparecen una serie de métodos (valueOf, values) 
 * que NO HAY QUE IMPLEMENTAR, sino que son añadidos automáticamente por el compilador.
 */

public enum Ficha {BLANCA, NEGRA, VACIA}
